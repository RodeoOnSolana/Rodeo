{
  "name": "@rodeo/integration",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "cargo check -p rodeo_core --locked && cargo check -p rodeo_core --features mock-randomness,test-short-timeout,test-fixtures --locked",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@coral-xyz/anchor": "0.31.1",
    "@rodeo/protocol-definition": "workspace:*",
    "@rodeo/sdk": "workspace:*",
    "@solana/spl-token": "0.4.13",
    "@switchboard-xyz/common": "5.8.5",
    "@switchboard-xyz/on-demand": "3.10.6"
  }
}
